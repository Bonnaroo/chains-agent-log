# OWNER SETUP — what Guillermo needs to do so the team runs smoothly (CEO keeps this current)

## Do once, now
- PRE-APPROVE TOOLS: open the Scheduled panel -> "Chains Office — on shift" -> Run now (once). This approves the
  browser + GitHub access so future unattended shifts don't stall on a permission prompt.

## Keep in mind (current fragility of the "this route" build)
- The 24/7 team works by driving your browser (clicking GitHub + Claude Design like a person). For shifts to run,
  the computer must be AWAKE with this app open, the Chrome extension connected, and you logged into GitHub +
  Claude Design. If the machine sleeps, missed shifts run on next launch. Narrow the hours anytime if usage is heavy.
- This browser-driving is the least robust part. PHASE 2 (the coding path) removes it — the team will use real code
  tools instead of clicking a browser.

## Later (CEO will prompt you when each is actually needed — not yet)
- Create a Firebase project for the public app (App B).
- Apple Developer ($99/yr) + Google Play ($25) accounts for store submission; a privacy policy.
