# Chains — Owner Inbox

Guillermo (or Claude on his behalf) writes new asks here as an unchecked line. Dispatcher converts each into a
GitHub Issue (labeled `source:owner`), writes the issue number next to the line, and checks it off. Never
re-import an already-checked line.

- [ ] (nothing pending right now — 2026-07-29 seed)
